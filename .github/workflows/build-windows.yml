name: Build Windows EXE & Upload to Release

# Thay đổi trigger: Chỉ chạy khi một Release mới được xuất bản
on:
  release:
    types: [published]

jobs:
  build-and-upload:
    runs-on: windows-latest
    
    # Cấp quyền ghi (write) để máy chủ được phép tự động upload file vào Release của bạn
    permissions:
      contents: write

    steps:
    - name: Lấy mã nguồn về máy chủ
      uses: actions/checkout@v4

    - name: Cài đặt Python 3.10
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Cài đặt thư viện Python
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller

    - name: Trích xuất Phiên bản từ main.py
      id: extract_version
      run: |
        VERSION=$(python -c "import re; print(re.search(r'CURRENT_VERSION\s*=\s*[\"\'\']([^\"\'\']+)[\"\'\']', open('main.py', encoding='utf-8').read()).group(1))")
        echo "APP_VERSION=$VERSION" >> $GITHUB_ENV
      shell: bash

    - name: Đóng gói thành file EXE bằng PyInstaller
      run: |
        pyinstaller --noconsole --windowed --collect-all customtkinter --name PDFScan2Word main.py

    - name: Nén file EXE thành ZIP
      run: |
        Compress-Archive -Path dist\PDFScan2Word.exe -DestinationPath PDFScan2Word-v${{ env.APP_VERSION }}-Windows.zip
      shell: powershell

    - name: Đính kèm file ZIP Windows vào bài Release
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        gh release upload ${{ github.event.release.tag_name }} PDFScan2Word-v${{ env.APP_VERSION }}-Windows.zip
      shell: bash